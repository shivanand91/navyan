# Use same-origin /api in production. Set VITE_API_URL in your hosting dashboard
# to your deployed backend URL (e.g. https://your-backend.onrender.com/api) if
# frontend and backend are on different domains.
